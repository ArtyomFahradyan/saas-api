import { ADMIN_AUTH } from '../../../configs/constants';

export default {
    getFiles: {
        authentication: true,
        authenticationType: ADMIN_AUTH
    },
    download: {
        // authentication: true,
        // authenticationType: ADMIN_AUTH
    }
};

